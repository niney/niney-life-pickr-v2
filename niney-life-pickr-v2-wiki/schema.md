---
name: niney-life-pickr-v2
mode: codebase
last_updated: 2026-05-19

---

# Wiki Schema

이 위키의 토픽 / 컨셉 명명 규약과 구조의 source of truth. 컴파일러는 이 파일을 보고 새 토픽을 만들 때 기존 슬러그를 우선 재사용한다.

## Conventions

- **Topic slug** — 워크스페이스 디렉터리명 또는 모듈 경계 기준 lowercase-kebab-case. 모노레포의 경우 `apps/<name>`/`packages/<name>` 디렉터리명을 그대로 사용. 도메인 단위로 더 좁히려면 `crawl`처럼 모듈명 단독 슬러그도 허용 (단, 충분한 양 + 활발한 활동이 있을 때).
- **Concept slug** — 패턴을 설명하는 짧은 구문 (`zod-ssot-buildless`, `sse-token-auth`). "결정의 결과"가 아니라 "결정 자체"가 슬러그가 되도록.
- **링크 스타일** — markdown (`[label](path.md)`). 위키 외부(루트, 소스)로 가는 링크는 상대 경로 `../../...`.
- **언어** — 본문은 한국어, 코드/식별자는 영어 그대로.
- **Coverage 태그** — 모든 섹션 헤딩에 `[coverage: high|medium|low — N sources]` 필수. 5+ sources = high, 2–4 = medium, 0–1 = low. Sources 섹션은 늘 high.

## Terminology — "웹" / "앱" / "모바일"

`apps/web`과 `apps/mobile`이 둘 다 모바일 컨텍스트를 가져서 "모바일"이라는 단어가 모호해진다. 다음 규칙으로 분리한다 (CLAUDE.md와 동일).

| 호칭 | 가리킴 | 코드 |
|---|---|---|
| **웹** | Vite + React 19 SPA (공개 + 어드민) | `apps/web` |
| **앱** | Expo + RN 앱 통합 | `apps/mobile` |
| **iOS앱** / **Android앱** | 앱의 네이티브 빌드 | `apps/mobile` (분기 파일 없음 — RN 공통) |
| **Expo Web** | 앱의 RN-Web 출력 (`expo start --web`) | `apps/mobile` |
| **모바일** | **웹**의 작은 화면 / 반응형 레이아웃 | `apps/web` |
| **모바일 단말** | 휴대전화 브라우저로 **웹** 접속 | — |

- 토픽 슬러그(`mobile`, `web`)와 디렉터리 식별자는 그대로 유지 — 영문 식별자는 변경 안 함.
- 한국어 본문에서 "모바일"은 **웹의 반응형**만 가리킨다. `apps/mobile`을 가리킬 땐 항상 "앱".
- 다음 컴파일부터 이 규칙을 적용. 기존 문서의 모호한 "모바일"은 컴파일러가 점진적으로 다시 쓴다.

## Topics

| Slug | 범위 | 핵심 위치 |
|---|---|---|
| `project-overview` | 모노레포 전체 — 디렉터리, 워크플로, 공통 결정 | `README.md`, `CLAUDE.md`, `TECH_STACK.md`, 루트 설정 파일 |
| `friendly` | Fastify 백엔드 (crawl·ai 제외) | `apps/friendly/` |
| `crawl` | Naver Place / 캐치테이블 / 다이닝코드 크롤러 모듈 | `apps/friendly/src/modules/crawl/` |
| `canonical` | 출처 가로지르는 같은 가게 묶기 (Naver/캐치테이블/다이닝코드) — 후보 매칭·merge·split·자동 검토 큐·자동 DC 머지 후크(C안) | `apps/friendly/src/modules/canonical/`, `apps/friendly/src/lib/matching.ts`, `apps/friendly/src/modules/restaurant/restaurant.merge.ts`, `packages/api-contract/src/schemas/canonical.ts`, `packages/shared/src/{api,hooks}/canonical.*`, `apps/web/src/components/restaurant/{CanonicalMergePanel,MergeProposalQueue}.tsx` |
| `auto-discover` | 어드민 자동 발견 잡 — 키워드 한 줄 + 카테고리 칩 + targetCount 입력 → AI 가 검색어 8 개 생성 → 다중 검색 + dedupe → 그룹 5 개씩 직렬 크롤·등록 | `apps/friendly/src/modules/auto-discover/`, `packages/api-contract/src/schemas/auto-discover.ts`, `packages/shared/src/{api/autoDiscover.api.ts,hooks/useAutoDiscover.ts,stores/activeAutoDiscoverJobStore.ts}`, `apps/web/src/{routes/admin/AdminAutoDiscoverPage.tsx,components/admin/auto-discover/}` |
| `ai` | LLM 통합 (Ollama Cloud) — provider config DB, 어댑터, 병렬 요청, admin UI 연동 | `apps/friendly/src/modules/ai/`, `packages/api-contract/src/schemas/ai.ts`, `apps/web/src/routes/admin/AdminAi*Page.tsx` |
| `web` | Vite + React 19 SPA | `apps/web/` |
| `mobile` | Expo 52 + RN 0.76 앱 | `apps/mobile/` |
| `menu-grouping` | 식당 단위 메뉴 표기 변형 → canonical 그룹 LLM 정규화 + 순위/긍부 통계 + batch 잡 SSE | `apps/friendly/src/modules/menu-grouping/` |
| `analytics` | 식당 가로지르기 글로벌 메뉴 머지 + 카테고리 트리(`categoryPath`) + 통계 API + admin 운영 UI | `apps/friendly/src/modules/analytics/`, `packages/api-contract/src/schemas/analytics.ts`, `apps/web/src/routes/admin/AdminAnalyticsPage.tsx` |
| `map` | vworld 지도 통합 — OpenLayers + WMTS 직접 호출, DB-backed 키 (`MapProviderConfig`), 어드민 단일 마커 + 공개 다중 마커 양쪽 사용 | `apps/web/src/components/restaurant/{MapCanvas,VWorldMap,PublicRestaurantsMap}.tsx`, `apps/web/src/lib/vworld.ts`, `apps/friendly/src/modules/settings/`, `packages/api-contract/src/schemas/settings-map.ts` |
| `api-contract` | `@repo/api-contract` Zod 스키마 SSOT | `packages/api-contract/` |
| `shared` | `@repo/shared` FE 공통 (API/hooks/store/UI) | `packages/shared/` |
| `utils` | `@repo/utils` 순수 헬퍼 | `packages/utils/` |
| `config` | `@repo/config` tsconfig + ESLint 베이스 | `packages/config/` |

## Concepts

| Slug | 연결 토픽 | 패턴 한 줄 |
|---|---|---|
| `zod-ssot-buildless` | api-contract, friendly, shared, web, mobile, utils, project-overview, ai, auto-discover | Zod 스키마 SSOT는 빌드 없는 src export와 한 묶음 — 스키마 1개 변경 → 모든 컨슈머 컴파일 타임 동기화 |
| `sse-token-auth` | friendly, crawl, shared, web, menu-grouping, analytics, auto-discover | EventSource 헤더 한계 → SSE만 `?token=` 쿼리 인증 + Pino 로거에서 정규식 리덕션 |
| `platform-ui-split` | shared, web, mobile, project-overview | 로직은 `@repo/shared`로 공유, UI는 `.web.tsx` / `.native.tsx`로 플랫폼 분기 — Tamagui/RN-Web 거부 |
| `workspace-package-resolution` | api-contract, friendly, shared, web, project-overview | `@repo/*` 컨슈머 도달 체인 — pnpm `injected` → vite extensionAlias → esbuild prebundle namespace re-export → autoload 우회. 한 단계 깨지면 컨슈머 import 에러로 일관 출몰 |
| `stream-driven-cache-merge` | crawl, friendly, shared, web, menu-grouping, analytics, auto-discover | SSE 이벤트가 머지 가능한 완성된 페이로드를 동봉 → `setQueryData`로 직접 패치, follow-up GET 0회 |
| `in-memory-singleton-gates` | ai, crawl, friendly, shared, menu-grouping, analytics, canonical, auto-discover | 외부 큐/Redis 없이 모듈 싱글턴 + in-memory FIFO로 동시성 제어 — AI cap, 크롤 큐, persistTail, SSE 매니저, 잡 registry 모두 같은 모양 |
| `versioned-llm-prompts` | ai, friendly, menu-grouping, analytics, auto-discover | 도메인 별 `*_VERSION` 상수 + DB 컬럼 + UI stale 배지 + 수동 재실행 — 프롬프트/스키마 변경을 데이터와 조약처럼 다룬다 (`ANALYSIS_VERSION=4`, `MENU_GROUPING_VERSION=1`, `GLOBAL_MERGE_VERSION=2`, `AUTO_DISCOVER_PROMPT_VERSION=1` — 마지막은 얇은 인스턴스) |
| `public-admin-route-split` | friendly, api-contract, shared, web, map, project-overview | 공개/어드민 같은 데이터에 가드만 토글하지 않고 라우트·스키마·훅을 페어로 분리 — 어드민 회귀 위험 0, 캐싱 정책 분리, 운영 메타 의도적 제거 (`Routes.X.list` 옆 `publicList`, `RestaurantListItem` 옆 `RestaurantPublicListItem`, `useRestaurantList` 옆 `useRestaurantsPublic`) |

## Topic Structure (article sections)

`.wiki-compiler.json`의 `article_sections`에 정의된 codebase 모드 8개 섹션. 모든 토픽 문서는 이 순서·이름을 유지한다.

1. **Purpose** — 모듈/서비스가 하는 일과 의존자
2. **Architecture** — 키 파일·구조·진입점
3. **Talks To** — 의존, 통신 패턴, 인터-서비스 호출
4. **API Surface** — 노출 엔드포인트·익스포트 함수·인터페이스
5. **Data** — 테이블·컬렉션·큐·캐시·상태
6. **Key Decisions** — 왜 이렇게 만들었나 (ADR/README 발췌)
7. **Gotchas** — 알려진 이슈·엣지 케이스·실패 모드
8. **Sources** — 기여한 모든 소스 파일 백링크

## Evolution Log

- **2026-05-07** — 초기 스키마 생성. 9개 토픽(project-overview, friendly, crawl, web, mobile, api-contract, shared, utils, config) + 3개 컨셉(zod-ssot-buildless, sse-token-auth, platform-ui-split)으로 시작. Codebase 모드, deep_scan=false. 사용자가 토픽/컨셉을 추가·이름 변경하려면 이 표를 직접 편집한 뒤 `/wiki-compile`을 다시 돌리면 된다.
- **2026-05-07** — `ai` 토픽 추가 (Ollama Cloud 통합 + 어드민 키/테스트 UI 도입과 함께). `workspace-package-resolution` 컨셉 추가 — AI 모듈 작업 중 `Routes.Ai` namespace re-export 우회·`vitest.config` extensionAlias·workspace symlink 깨짐을 정리하면서 cross-cutting 함정으로 식별됨. `zod-ssot-buildless`의 연결 토픽에 `ai` 추가 (같은 SSOT 패턴이 신규 도메인에서도 그대로 적용됨).
- **2026-05-07** — 맛집 도메인 (DB 영속화 + AI 요약 + 다중 크롤 + SSE 멀티플렉싱) 통합으로 `crawl`/`friendly`/`shared`/`web`/`api-contract` 5개 토픽 갱신. 신규 컨셉 2개: `stream-driven-cache-merge` (SSE 페이로드 직접 머지로 detail GET 회피), `in-memory-singleton-gates` (Redis 없이 모듈 싱글턴 + FIFO로 cap·순서·통합 모두 처리). `sse-token-auth`에 멀티플렉싱 `summaryEvents` instance 추가.
- **2026-05-08** — 미디어/요약 강화 (썸네일 프록시·동영상 분리·구조화 분석·재시도/백오프) 통합으로 `crawl`/`friendly`/`ai`/`api-contract`/`web`/`shared`/`utils` 7개 토픽 갱신. 신규 토픽·컨셉 없음 (기존 구조에 자연스럽게 흡수). 컨셉 2개에 신규 인스턴스 추가: `stream-driven-cache-merge`에 `VisitorReview.videos`·요약 분석 필드(SSE 페이로드 확장만으로 머지 인프라는 동일) / `in-memory-singleton-gates`에 placeId별 summary run 직렬화·Ollama 429 슬롯-보유 백오프(외부 게이트와 내부 회복 분리). `friendly`에 신규 media 모듈 (`/api/v1/media/thumbnail` Naver CDN 호스트 allowlist + sharp + 디스크 캐시) 흡수. `summary` 모듈은 활동량이 크지만 아직 friendly 내부 모듈로 유지 — 별도 토픽 분리는 다음 라운드에서 재평가.
- **2026-05-09** — 메뉴 분석 파이프라인 확장 (식당 단위 그룹핑 + 식당 가로지르기 글로벌 머지 + 카테고리 트리). 신규 토픽 2개: `menu-grouping` (식당당 1회 LLM 호출로 메뉴 표기 변형 정규화 + 순위/긍부 통계 + batch 잡 SSE), `analytics` (전역 메뉴 머지 두-패스 + categoryPath 단일 컬럼 + 메모리 트리 빌더 + 검색·필터 API). 갱신 토픽 7개: `friendly`(두 모듈 흡수 + summary v4 traits + getInsights MenuCanonical 기반), `api-contract`(menu-grouping/analytics 스키마 + Routes.Analytics + 첫 z.lazy 재귀 `CategoryTreeNode`), `shared`(잡 단위 SSE 훅 2개 — 공유 매니저 없이 hook 안에서 EventSource 라이프사이클 직접 관리), `web`(AdminAnalyticsPage 4섹션 + `?menu=`/`?category=` URL 동기화 useEffect 회피 + MenuRankingSection 비교 위젯 deep-link), `mobile`(맛집 탭 + 식당 상세 라우트 + MenuRankingCard, ADMIN gated), `ai`(LLM 컨슈머 3부류로 확장, 도메인별 VERSION 차이만), `project-overview`(3단계 분석 파이프라인 + 5개 신규 테이블). 신규 컨셉 1개: `versioned-llm-prompts` (`*_VERSION` 상수 + DB 컬럼 + UI 배지 + 수동 재실행 — 3 도메인이 같은 모양). 기존 컨셉 4개에 신규 인스턴스 추가: `in-memory-singleton-gates`(잡 registry 두 종 — multi-job actor 격리 / single-job inflight 가드), `sse-token-auth`(잡 SSE 두 엔드포인트), `stream-driven-cache-merge`(useGroupingJob/useGlobalMergeJob 의 snapshot+chunk/item 머지), `zod-ssot-buildless`(첫 z.lazy 재귀 + SSE event payload 개별 스키마). `docs/menu-hierarchy.md`(구현 완료 문서로 보존).
- **2026-05-09** — 공개 사용자 페이지 도입 (사용자 대상 맛집 탐색) + vworld 지도 통합. 신규 토픽 1개: `map` (vworld JS SDK 거부 → OpenLayers + WMTS XYZ 타일 직접 호출, 도메인 화이트리스트 회피, `MapProviderConfig` DB-backed 키, `MapCanvas`/`VWorldMap`/`PublicRestaurantsMap` 어드민·공개 양쪽 사용). 갱신 토픽 5개: `friendly`(공개 라우트 3개 + settings 모듈 신규, `getPublicList`/`getPublicDetail` 분리), `api-contract`(`RestaurantPublicListQuery/Item/Result` + `PublicReviewAnalysis`/`PublicVisitorReview`/`RestaurantPublicDetail` + `MapProviderPublicConfig` zod 페어 신규), `shared`(`useRestaurantsPublic`/`useRestaurantPublic`/`useRestaurantPublicInsights`/`useMapPublicConfig` + `restaurantApi.public*`/`settingsMapApi.publicConfig`), `web`(공개 `/restaurants` 풀 뷰포트 3-column + 5탭 상세 패널 `panel/`→`detail/` 명명 정리 + `MapCanvas`/`PublicRestaurants*` 컴포넌트 + URL state 동기화 + `ImgWithFallback` 공용화 referrer-policy 회피 + Pretendard 공개 한정 + 텍스트 시프트 전역 + `/admin/settings` 탭 통합 + `AdminMapKeysPage`), `project-overview`(공개 vs 어드민 분리 정책 정착 + `MapProviderConfig` 테이블 + `ol@^10.7.0` 의존성). 신규 컨셉 1개: `public-admin-route-split` (공개/어드민 가드만 토글하지 않고 라우트·스키마·훅 페어 분리 — `Routes.X.publicList` ↔ `Routes.X.list`, `RestaurantPublicListItem` ↔ `RestaurantListItem`, `useRestaurantsPublic` ↔ `useRestaurantList`. 어드민 회귀 위험 0, 운영 메타 의도적 제거, 캐싱 정책 분리). 기존 컨셉 1개에 신규 인스턴스 추가: `zod-ssot-buildless`(map 도메인 4종 스키마 + 공개 맛집 페어 6종이 같은 SSOT 모델로 흡수 — 신규 토픽이 늘어도 컨슈머 4-5개가 컴파일 타임 동기화).
- **2026-05-14** — 모바일 UX 대규모 정비 (공개 맛집 + 어드민 발견 + 관리자 페이지). 신규 토픽·컨셉 0개 — 모든 변경 `web` + `project-overview` 두 토픽으로 흡수. 갱신 토픽 2개. 신규 knowledge file 1개: `docs/mobile-public-restaurant-ux.md` (모바일 sticky·body 스크롤 패턴의 SSOT 문서). 핵심 패턴: (1) 모바일 = body 스크롤 / xl+ = sticky 컬럼 — `/restaurants` 와 `/admin/discover` 양쪽 동일 구조, (2) `/restaurants/:placeId` 별도 라우트 + URL `?tab=` 푸시 라우팅 — 모바일 뒤로가기 = 이전 탭, (3) sticky element wrapping 금지 / `overflow:auto` 컨테이너 밖 sticky — containing block 트랩 회피, (4) `100vh` → `100dvh`, (5) 한글 IME `compositionStart/End` + 로컬 draft state — controlled value 가 미완성 조합 덮어쓰는 "ㅇ으음" 회피, (6) Lightbox 네이티브 scroll-snap 캐러셀 (JS swipe 핸들러 제거 — 브라우저 momentum/snap 활용), (7) iOS Safari focus 자동 줌 차단 — Input 컴포넌트 `text-base sm:text-sm` + `@media (max-width:639px)` 글로벌 `font-size:16px` 룰, (8) Card 컴포넌트 모바일 `p-4 sm:p-6` (콘텐츠 가용 폭 ~12% 회수), (9) 리뷰 카드 LLM 분석 풀 노출 — 메뉴별 좌측 sentiment stripe + tips muted 박스 + keywords 칩 + SatisfactionChip(컬러 도트 + 1~5 점수), 이미지 `h-56 sm:h-64` 가로 스냅 캐러셀 + 클릭 → Lightbox, (10) 관리자 모바일 — AdminLayout `<md` fixed 드로어 + backdrop / `md+` sticky aside, AdminTopBar 햄버거 + "일반 화면으로" 우측 이전, AdminDiscoverPage 패널/지도 토글 + 검색 탭 selection > 0 시 토글 자동 `bottom-20` 상승 (sticky 크롤바 위로). 신규 컨셉 0개 — 모바일 UX 패턴은 `web` 한 토픽에 닫혀 있어 (3+ 토픽 연결 임계 미달) 컨셉화 미평가. `docs/mobile-public-restaurant-ux.md` 가 그 SSOT 역할. 기존 컨셉 신규 인스턴스 없음 — 라우트 분리는 어드민/공개 페어가 아닌 내부 list/detail 분리라 `public-admin-route-split` 와 다른 결.
- **2026-05-09 (follow-up)** — 어드민 발견 페이지 도입 (`/admin/discover` — Playwright 기반 네이버 PC 지도 검색 + 다중 선택 일괄 크롤링 + 좌/우 패널 토글). 신규 토픽·컨셉 0개 — 모두 기존 토픽으로 흡수. 갱신 토픽 7개: `crawl`(`naver-search.playwright.adapter.ts` 신규 — captcha-aware capture / `searchPlaces()` 메서드 + GET `/admin/crawl/search` / **actor 단위 rate-limit 완전 제거** — `RATE_LIMIT_WINDOW_MS` 상수·`lastCallByActor: Map`·검사 블록 모두 삭제), `friendly`(crawl 모듈 변경 흡수 + 검색 어댑터 별도 Browser 인스턴스), `api-contract`(`NaverSearchResult` / `CrawlSearchQuery` / `CrawlSearchResult` + `Routes.Crawl.search` zod 페어 신규), `shared`(`useNaverSearch(q, bbox)` + `crawlApi.search` 신규), `web`(`AdminDiscoverPage` + `DiscoverMap`/`DiscoverPanel` 컴포넌트 + `/admin/discover` 라우트 + AdminLayout "맛집 발견" 메뉴 신규 / `panelPrefsStore` + `usePanelSide(key)` 좌/우 토글 어드민 발견 + 공개 맛집 양쪽 적용 / `MapCanvas` `MapMarker.variant: 'primary' | 'muted'` + `ResizeObserver` 자동 reflow / `RestaurantsPage` 좌/우 토글 적용), `map`(MapCanvas variant 핀 색 분기 + ResizeObserver 자동 reflow), `project-overview`(어드민 발견 페이지 정책 + actor rate-limit 제거 정책 + 패널 좌/우 토글 namespace 정책). 기존 컨셉 3개에 신규 인스턴스 추가: `in-memory-singleton-gates`(잘못된 게이트 제거 학습 — 윈도우 기반 rate-limit 은 다중 시작 패턴과 충돌, 게이트가 dedup + max_concurrent 두 layer 로 충분하면 윈도우는 빼라), `public-admin-route-split`(페어 경계 흐림 첫 사례 — 어드민 발견이 공개 hook `useRestaurantsPublic` + `PublicRestaurantDetail` 컴포넌트를 차용. 어드민 응답 셋이 좌표를 노출 안 해서 — 분리의 합당성을 흔들지 않음), `zod-ssot-buildless`(검색 스키마 페어 추가 — `source: z.enum(['playwright'])` 어댑터 fallback 노출 enum). source 6 추가 (`naver-search.playwright.adapter.ts` / `dev-capture-search.ts` / `AdminDiscoverPage.tsx` / `DiscoverMap.tsx` / `DiscoverPanel.tsx` / `panelPrefsStore.ts`).

- **2026-05-17** — 어드민 **맛집 자동 발견** 도입 + canonical 자동 DC 매칭 후크(C안) + 어드민 발견 상세 Naver+DC 융합 + 서버 안정성 패치 (boot 시 stale 요약 정리 + summary SSE heartbeat/idle timeout). **신규 토픽 1개**: `auto-discover` (키워드 한 줄 + 카테고리 칩 + targetCount 입력 → AI 가 검색어 8개 생성 → 다중 검색 + dedupe + 이미 등록된 placeId 제외 → 그룹 5개씩 직렬 크롤+5병렬, per-actor 1잡 정책, AI 미설정 시 결정론적 fallback 키워드). 갱신 토픽 7개: `friendly`(auto-discover 모듈 + `restaurant.merge.ts` 융합 헬퍼 군 + `cleanupStaleReviewSummaries` boot hook), `crawl`(`MAX_CONCURRENT_PER_ACTOR` 3→5 — 자동 발견 그룹 크기와 일치, `tryAutoMatchDiningcode` 후크 — Naver done 후 임계 통과 시 DC 자동 저장+머지, diningcode 리뷰에 AI 요약 join), `canonical`(자동 머지 정책 진화 — "수동 확정만" → "C안 자동 머지 + 임계 못 넘는 케이스만 검토 큐 fallback", 임계 상수 3개 노출), `web`(`/admin/auto-discover` 라우트 + 폼/카드 컴포넌트, 발견 상세 패널 Naver+DC 융합 렌더, 발견 리스트 카드 DC 형제 카운트 합산, 호버 자동 이동 → "지도" 버튼 클릭, DC 상세 페이지 리뷰에 AI 요약), `api-contract`(`auto-discover.ts` 신규 14 export + `restaurant.ts` fused detail 6 신규 export + `crawl.ts` `summaryText` 필드 — 두 enum 분리 `JobState` / `Phase` + 첫 `cancelled` enum 값), `shared`(autoDiscover 트리오 — api/hook/store + summarySseManager heartbeat watchdog + idle timeout + delta-aware `(snap, prev)` snapshot 핸들러 — list 카운트 client-augmentation 보호), `project-overview`(자동 발견 워크플로 정책 + C안 자동 머지 정착 + SSE liveness 패턴). 기존 컨셉 5개에 신규 인스턴스 추가: `in-memory-singleton-gates`(autoDiscoverRegistry — 9번째 인스턴스, two-channel publish + per-actor 단일잡 + `MAX_CONCURRENT 3→5` 게이트 cap 자체를 컨슈머 디자인에 맞춤 / `cleanupStaleReviewSummaries` boot hook — 게이트의 재시작 회복 패치), `sse-token-auth`(auto-discover events 8번째 SSE 엔드포인트 + summary heartbeat liveness 보강), `stream-driven-cache-merge`(`useAutoDiscoverJob` — 머지 패턴에 클라 자체 카운트 계산 추가 + snapshot 핸들러 `(snap, prev)` 시그니처로 client-augmented 필드 보존), `zod-ssot-buildless`(auto-discover 14 export + fused detail 페어 6 export 한 라운드 흡수 — `cancelled` enum 첫 등장 + state/phase 두 축 분리), `versioned-llm-prompts`(`AUTO_DISCOVER_PROMPT_VERSION=1` — 얇은 인스턴스, 4 요소 세트 중 상수만 채택 / DB 컬럼·UI 배지·재실행 없음, AI 출력이 영속화되지 않아 해당 없음 — 패턴이 새 도메인 기본 디자인으로 자리잡았음 시사). **신규 컨셉 0개** — 자동 발견의 Phase 1/2/3 직렬 그룹 + AI fallback 패턴이 흥미롭지만 한 도메인에 닫혀 있어 (3+ 토픽 임계 미달) 컨셉화 보류. 같은 모양이 다른 잡 도메인으로 번지면 추출. heartbeat + idle timeout 도 현재 summary SSE 한 곳뿐 — 다른 잡 훅으로 번지면 새 컨셉 후보. 신규 source 14개: auto-discover 모듈 5 파일, `restaurant.merge.ts`/`.test.ts`, `auto-discover.ts` schema, `autoDiscover.api.ts`/`useAutoDiscover.ts`/`activeAutoDiscoverJobStore.ts`, `AdminAutoDiscoverPage.tsx`/`AutoDiscoverForm.tsx`/`AutoDiscoverJobCard.tsx`. `auto-discover` 가 신규 토픽 + 5 개 기존 컨셉의 인스턴스 — 적당한 크기로 자체 토픽 분리 정당화.

- **2026-05-19** — **12th compile** — 잡 단계별 영속 로그 시스템 + 요약 라이프사이클 6 상태(`queued`/`cancelled` 추가) + 부팅 자동 재큐잉 + SummaryService 앱 전역 singleton + 모바일 v2 리빌드(네이티브 탭바 + dev client + 맛집 탭 통합 + WebView vworld) + 공개 맛집 위치 기반 첫 진입 + 공개 리뷰 페이지네이션 분리. **신규 토픽·컨셉 0개** — 모두 기존 토픽/컨셉으로 흡수. 갱신 토픽 9개: `friendly`(CrawlJobLog 시스템 — `JobLogService` 세-채널 fan-out + `(jobId, seq)` dedup, `plugins/summaries.ts` 앱 전역 singleton — SummaryService/JobLogService/AiConfigService 셋 `app.decorate`, `ReviewSummaryStatus` 6 상태 enum 확장, `cancelSummaryForPlace`/`resumeSummaryForPlace` 서비스 메소드, 부팅 시 `cleanupStaleReviewSummaries` + `rescheduleStaleSummaries` boot hook 으로 stale 행 자동 청소+재큐잉, 신규 라우트 `cancelSummary`/`resumeSummary`/`crawlLogs(:placeId)`/`Routes.Crawl.jobLogs(:id)`), `crawl`(`job-log.service.ts` 신규 + `CrawlEvent.log` variant + `summary.service.ts` 가 stage 로그 채널 `'summary'` 로 흘림), `web`(SummaryProgressSection cancel/resume 2 버튼 + 7 배지, ActiveJobPanel 진행도/로그 탭 분리 + `JobLogTab` 신규, 상세 페이지 `RestaurantCrawlLogsSection` 아코디언 + LIVE Radio 배지, `RestaurantsV2Page` + `restaurant-v2/BottomSheet` 풀스크린 지도+바텀시트 패턴, 위치 기반 자동 fly + "내 위치" 버튼, detail/ 디렉터리 통합 — 모바일 v2 와 같은 컴포넌트 트리, 공개 리뷰 chip+sort URL 동기화 + 페이지네이션 분리, `PublicLayout`/`PublicSidebar`/`PublicTopBar` 신규), `mobile`(**대규모 리빌드** — 네이티브 `(tabs)` 그룹, dev client 기본 + `EXPO_PUBLIC_API_URL` 직접 읽기, 맛집 탭 통합 UX(풀스크린 WebView 지도 + 바텀시트 + 상세 in-sheet + list/detail 2-sheet 적층), `PublicRestaurantsWebMap.native.tsx` 가 WebView 안 vworld HTML 주입 + `.web.tsx` 가 web 컴포넌트 재사용, `restaurantDetail/` 디렉터리 풀세트, `useUserLocationNative` + `expo-location`, 마커 fly+zoom + Reanimated 워클릿 폭주 fix(selection/marker 채널 분리), `with-android-minify`/`with-swift-concurrency-fix` config plugins, 콜드 스타트·이미지 렌더링 최적화 3종, NotchFade), `shared`(`useUserLocation` 웹 훅 + `useCancelSummary`/`useResumeSummary`/`useRestaurantCrawlLogs`/`useCrawlJobLogs` + `useRestaurantSummaryEvents` 시그니처 확장(`onLog` 콜백 ref 안정화), `summarySseManager` LogHandler + 'log' named event 라우팅, `useCrawl` `logs[]` reducer 가 `(jobId,seq)` Map dedup, `useRestaurantPublicReviews` 신규 infiniteQuery, `restaurantApi.crawlLogs`/`cancelSummary`/`resumeSummary`/`publicReviews` + 옛 백엔드 응답 어댑팅), `api-contract`(`CrawlLogLevel`/`CrawlJobLogEntry`/`CrawlJobLogsQuery|Result`/`CrawlEvent.log` variant, `ReviewSummaryStatus` 4→6 enum 확장, `RestaurantCancelSummaryResult`/`RestaurantResumeSummaryResult`/`RestaurantSummaryLogEvent`, `RestaurantPublicReviewsQuery|Result` + sentiment/sort enum + `RestaurantPublicDetail` 의 `reviewsFirstPage`/`reviewCounts` 분리, `PublicVisitorReview` 분리, Routes 5 신규), `utils`(`geo.ts` 신규 — `LatLng`/`Bbox` 타입 + `computeBboxAround`/`isInKorea`), `map`(`PublicRestaurantsWebMap.native.tsx`/`publicRestaurantsMapHtml.ts` WebView 패턴, 위치 기반 자동 fly + "내 위치" 버튼, 마커 fly+zoom + 비선택 dot/선택 핀+라벨 스타일 분기), `project-overview`(잡 로그 시스템 정책 + 요약 6 상태 + queued 도입 동기(placeId=36668856 사고) + plugin singleton 패턴 + 모바일 v2 워크플로 + WebView 지도 패턴). 기존 컨셉 3개에 신규 인스턴스 추가: `in-memory-singleton-gates`(`plugins/summaries.ts` 앱 전역 plugin singleton 패턴 신규 — 모듈 import 가 인스턴스 보장의 충분조건 아닐 때 다음 단계 / `cancelledPlaces` Set "사용자 의도 표식" — cancel/resume 토글이 in-memory Set + DB 상태로 표현 / `cleanupStaleReviewSummaries` where 절 확장(queued 포함) + `rescheduleStaleSummaries` 자동 재큐잉 — chain 휘발 윈도우를 49 분 → ms 로 줄임), `stream-driven-cache-merge`(CrawlJobLog "2-fan-out 1-dedup" 변형 — 한 origin 이벤트가 두 SSE + 한 DB 로 vector, 같은 모노톤 `seq` 가 박혀 클라가 `(jobId, seq)` Map 으로 통합 dedup, 영구 누적 케이스라 invalidate 없이 `nextCursor` 페이지네이션이 그 역할 — 페이로드 설계가 머지를 가능케 하는 가장 강한 인스턴스: 서버가 ID 공간 (jobId, seq) 을 미리 발급해 멀티 채널 dedup 위탁), `platform-ui-split`(`useUserLocation` 웹/`useUserLocationNative` 네이티브 페어 + `@repo/utils/geo` 공통 산술 — 인터페이스 동형 + 공통 함수 추출로 인프라 분기 흡수 / `PublicRestaurantsWebMap.native.tsx` + WebView vworld HTML 주입 + `.web.tsx` 재사용 — 한 컴포넌트가 세 빌드에서 다른 인프라로 같은 표현. 연결 토픽에 `utils`, `map` 추가). **신규 컨셉 0개** — 잡 로그 시스템의 "2-fan-out 1-dedup" 이 흥미롭지만 기존 `stream-driven-cache-merge` 의 변형으로 흡수 가능, 패턴이 다른 도메인으로 번지면 별도 컨셉 추출 후보. 신규 source 6개: `apps/friendly/src/plugins/summaries.ts`, `apps/friendly/src/modules/crawl/job-log.service.ts`, `apps/friendly/prisma/migrations/20260518014530_add_crawl_job_log/migration.sql`, `packages/shared/src/hooks/useUserLocation.ts`, `packages/utils/src/geo.ts`, `apps/mobile/src/hooks/useUserLocationNative.ts`. Prisma 마이그레이션 1 개 추가(`CrawlJobLog` 테이블).

- **2026-05-15** — 다이닝코드/캐치테이블 출처 추가 + canonical 그룹핑 시스템 + DC 일괄 저장 SSE 통합. **신규 토픽 1개**: `canonical` (출처 가로지르는 같은 가게 묶기 — 후보 매칭·merge·split·자동 검토 큐·canonical 단위 삭제, **자동 머지 없음** 정책). 갱신 토픽 6개: `crawl`(캐치테이블 search/shop adapters + 다이닝코드 search/shop HTTP adapters + `diningcode-bulk-save-registry.ts` + `runDiningcodeBulkSave` 직렬 vRid 루프 + naver-search 가 Playwright→HTTP 어댑터로 이전), `friendly`(canonical 모듈 + `lib/matching.ts` bigram Jaccard + Haversine + 4개 Prisma 마이그레이션 + Restaurant `(source, sourceId)` split + `canonicalId` FK NOT Cascade), `web`(AdminDiningcodePage 정식 페이지 + AdminCatchtable*/AdminDiningcodeShopPage + CanonicalMergePanel + MergeProposalQueue + AdminRestaurantsPage canonical 그룹 리스트 + suggestion 알림 + canonical 단위 삭제), `shared`(useCanonical 9훅 + DC bulk save SSE 훅 + `activeDiningcodeBulkSaveJobStore` + summarySseManager 가 canonicalId 확장), `api-contract`(`canonical.ts` 14 스키마 + `crawl.ts` 에 catchtable·diningcode·bulk-save 다수 + `restaurant.ts` `CanonicalListItem` 그룹핑 shape), `project-overview`(3 출처 + canonical 1:N 정책 + 자동 머지 없음 + AdminDiningcodePage 정식/테스트 분리). 기존 컨셉 3개에 신규 인스턴스 추가: `sse-token-auth`(DC bulk save events — 7번째 SSE endpoint, `buildDiningcodeBulkSaveEventsUrl` 가 `useGroupingJob` 빌더와 동형), `stream-driven-cache-merge`(`useDiningcodeBulkSaveJob` — `useGroupingJob` 의 카피 "패턴 머지 카피의 첫 사례"), `in-memory-singleton-gates`(`diningcodeBulkSaveRegistry` — 8번째 인스턴스, `groupingJobRegistry` 동형이지만 per-actor **단일 잡** 정책; 연결 토픽에 `canonical` 추가). **신규 컨셉 0개** — canonical 의 "자동 매칭 후보 → 어드민 검토 큐 → 수동 확정" 패턴이 흥미롭지만 아직 한 도메인에 닫혀 있어 (3+ 토픽 임계 미달) 컨셉화 보류. 같은 패턴이 LLM 평가/그룹 머지 큐로 번지면 그 때 추출. Prisma 신규 테이블 2개(`CanonicalRestaurant`, `CanonicalMergeProposal`) + Restaurant `(source, sourceId)` 분리 + `suggestionDismissedAt` (마이그레이션 4개). 신규 source 다수: canonical 모듈(`canonical.service.ts`/`canonical.route.ts`/`canonical.test.ts`/`proposal.service.ts`), `lib/matching.ts`/`matching.test.ts`, `diningcode-bulk-save-registry.ts`, 4개 adapters(catchtable-search/shop, diningcode-search/shop), Web 5개 페이지(AdminDiningcodePage/ShopPage/TestPage, AdminCatchtableTestPage/ShopPage), 2개 컴포넌트(CanonicalMergePanel/MergeProposalQueue), `useCanonical.ts`, `canonical.api.ts`, `activeDiningcodeBulkSaveJobStore.ts`. `crawl` 토픽이 11 라우트 + 4 어댑터 + 2 잡 패턴으로 가장 큰 토픽이 됨 — 다음 라운드에 캐치테이블이 더 늘면 별도 토픽 분리 후보.
