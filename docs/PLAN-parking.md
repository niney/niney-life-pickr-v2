# 주차(/parking) — 주차장·전기차 충전소·공항 주차

사이드바 **주차** 메뉴(대중교통 다음). 지도 + 패널(바다·집값과 같은 골격), 상단 탭 **주차장 | 충전소 | 공항**.
식당 상세 **가는 법** 탭에 주차 섹션 + 홈 탭 한 줄 요약. 실시간 값은 5분마다 받아 요일·시간 칸에 누적(혼잡 이력).
웹 먼저, 앱(apps/mobile)은 다음 단계. 조사 근거·후보 ID 는 메모리 `project_parking_data_survey`.

## 결정 (2026-09-25 사용자)

| 항목 | 결정 |
|---|---|
| 위치 | 일상지도 레이어가 아니라 별도 메뉴 `/parking` |
| 구성 | 지도 + 패널, 탭 주차장 · 충전소 · 공항 |
| 맛집 | 가는 법 탭 '주차' 섹션 + 홈 탭 한 줄 요약 |
| 혼잡 이력 | 지금부터 축적(표시는 표본이 쌓인 뒤 자동) |
| 체류 시간별 요금 정렬 | 이번엔 안 함(상세의 예상 요금만) |

## 데이터 원천

| 원천 | 키·승인 | 쓰임 | 비고 |
|---|---|---|---|
| 전국주차장정보표준데이터 15012896 `tn_pubr_prkplce_info_api` | DATA_GO_KR, 자동승인 | 전국 공영 위주 1.9만 곳 정적 | 좌표 96%, 요금 72%. 1,000행/콜 → 19콜 |
| 서울 공영주차장 안내 `GetParkInfo` | SEOUL_OPEN(기존) | 서울 공영 850곳(2,189행) | 노상은 구획마다 행(좌표만 다름) → 코드로 접음. 좌표는 시영 117곳뿐 → 지오코딩 |
| 서울 시영 실시간 `GetParkingInfo` | SEOUL_OPEN | 122곳 현재 대수(109곳 연계) | 1콜 전량, 수 분 단위 갱신 |
| 한국공항공사 주차 혼잡도 15158689 `parking-congestion/info` | DATA_GO_KR, 자동 | 13개 공항 25개 주차장 | 좌표 없음 → 공항 좌표 상수 |
| 인천공항 주차 15095047 `StatusOfParking/getTrackingParking` | DATA_GO_KR, 자동(개발 1,000/일) | 19개 구역 | 현재 대수가 면수를 넘기도 함(초과 주차) |
| 전기차 충전소 15076352 `EvCharger` | DATA_GO_KR, 자동(개발 1,000/일), 공공누리 1유형 | 충전기 52.5만 기 | 전량 53콜(9,999행) / 상태 변경분 period≤10분 |
| KOTSA 주차정보 15099883 | 심의승인 대기 | (승인 후) 시설·운영·실시간 | 파일 분석상 실시간 실연계 ≈4.2천·더미성 행 99% — 승인 후 재프로브 |

## 호출량(개발계정 기준, 서버 1대)

- 주차 실시간 폴러 5분: 서울 1 + 공항공사 1 + 인천 1 = 하루 각 288콜.
- 충전기 상태 폴러 10분(period=10, 9,999행 × 2페이지): 하루 ~288콜. 전량 적재 53콜은 스크립트(수동/배포).
- 로컬 dev 서버도 같은 키로 폴링하므로 두 대가 돌면 두 배 — 인천·충전기 1,000/일 안.
- 끄기: `PARKING_LIVE_CRON=`·`EV_STATUS_CRON=` 빈 값.

## 구조

- DB: `ParkingLot`(원천 접두 id `std:`/`seoul:`, 전량 교체) · `ParkingOccupancyStat`(key×요일×시 누적, 상한 = 키 수×168)
  · `EvStation`/`EvCharger`(전량 교체 + 상태 폴러 갱신) · `ParkingSync`(적재 이력).
- 중복: 표준데이터의 서울 행이 서울 원천과 겹치면(100m 안 + 이름 유사도) 서울 행을 남긴다.
- 조회: 일상지도와 같은 점/격자 셀(bbox·줌 임계) + LRU. 실시간은 메모리(폴러가 채움)를 응답에 합친다.
- 적재: `load:parking-lots [--sources=std,seoul] [--offline]` · `load:ev-chargers`. 상태 확인 `status:parking`.

## 차수

1. 계약·유틸·DB·주차장 적재(서울+표준) + 실시간 폴러·이력 + 공항 + 라우트·테스트
2. 전기차 충전소(적재·상태 폴러·라우트·테스트)
3. 웹 `/parking` 3탭 + NAV
4. 식당 가는 법 주차 섹션 + 홈 요약(리뷰 '주차' 관점·팁 집계 라우트)
5. docs/api 재생성 · deploy.sh · 메모리/위키

## 함정

- data.go.kr 키는 Encoding 값 — `toServiceKeyPart`(이중 인코딩 금지).
- 서울 `GetParkInfo` 유무료 코드는 `Y=유료/N=무료`(이름과 반대 느낌), 토·공휴일 무료는 SAT_/LHLDY_ 필드 별도.
- 운영시간 `0000-0000` 은 정보 없음, `2400` 은 자정.
- 공휴일은 판정하지 않는다(일요일만 공휴일 시간으로) — 상세에 표시.
